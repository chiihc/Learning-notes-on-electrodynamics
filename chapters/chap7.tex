\cleardoublepage

\section{守恒定律}
\subsection{电荷和能量}
连续性方程:
\begin{equation}
    \frac{\partial \rho}{\partial t}=-\boldsymbol{\nabla}\cdot\boldsymbol{J}
\end{equation}
假设我们有电荷和电流分布，在时刻t，它们产生电场$\boldsymbol{E}$和磁场$\boldsymbol{B}$。在下一时刻，$\dif t$，电荷
移动了一小段距离。问：在时间间隔$\dif t$中，作用在这些电荷上的电磁力做了多少功?
\begin{align*}
    \boldsymbol F\cdot\dif\boldsymbol{l}&=q(\boldsymbol{E}+\boldsymbol{v}\times\boldsymbol{B})\cdot\boldsymbol{v}\dif t\\
    \boldsymbol F\cdot\dif\boldsymbol{l}&=q\boldsymbol{E}\cdot\boldsymbol{v}\dif t\\
    P&=\int \boldsymbol{E}\cdot\boldsymbol{J}\dif\tau\\
    \boldsymbol{E}\cdot\boldsymbol{J}&=\frac{1}{\mu_0}\boldsymbol{E}\cdot(\boldsymbol{\nabla}\times\boldsymbol{B})
    -\varepsilon_0\boldsymbol{E}\cdot\frac{\partial \boldsymbol{E}}{\partial t}\\
    \boldsymbol{E}\cdot\boldsymbol{J}&=-\frac{1}{\mu_0}\boldsymbol{\nabla}\cdot(\boldsymbol{E}\times\boldsymbol{B})
    +\frac{1}{\mu_0}\boldsymbol{B}\cdot(\boldsymbol{\nabla}\times\boldsymbol{E})
    -\varepsilon_0\boldsymbol{E}\cdot\frac{\partial \boldsymbol{E}}{\partial t}\\
    \boldsymbol{E}\cdot\boldsymbol{J}&=-\frac{1}{\mu_0}\boldsymbol{\nabla}\cdot(\boldsymbol{E}\times\boldsymbol{B})
    -\frac{1}{\mu_0}\boldsymbol{B}\cdot\frac{\partial \boldsymbol{B}}{\partial t}
    -\varepsilon_0\boldsymbol{E}\cdot\frac{\partial \boldsymbol{E}}{\partial t}\\
    \boldsymbol{E}\cdot\boldsymbol{J}&=-\frac{1}{\mu_0}\boldsymbol{\nabla}\cdot(\boldsymbol{E}\times\boldsymbol{B})
    -\frac{1}{2\mu_0}\frac{\partial B^2}{\partial t}
    -\frac{\varepsilon_0}{2}\frac{\partial E^2}{\partial t}\\
    P&=-\frac{\dif}{\dif t}\int\frac{1}{2}\frac{\partial }{\partial t}\left(\varepsilon_0E^2+\frac{1}{\mu_0}B^2\right)\dif\tau-\frac{1}{\mu_0}\oint\boldsymbol{E}\times\boldsymbol{B}\cdot\dif\boldsymbol{a}
\end{align*}
单位时间内电磁场通过单位表面积向外传递的能量称为坡印廷矢量$\boldsymbol{S}$
\begin{equation}
    \boldsymbol{S}\equiv\frac{1}{\mu_0}\boldsymbol{E}\times\boldsymbol{B}
\end{equation}
如果我们用$u_{mech}$表示机械能能量密度，同时用$u_{em}$表示电磁场的能量密度：
\begin{equation}
    \frac{\partial u_{mech}+u_{em}}{\partial t}=-\boldsymbol{\nabla}\cdot\boldsymbol{S}
    \label{坡印廷_守恒定律}
\end{equation}
\mysssec{计算沿着电缆传输的功率（单位时间内传输的能量），假设内外两
个导线间的电势差为$U$。\\
(a)一个长同轴电缆带有电流$I$(电流沿着半径为$a$的内圆柱的表面流出，然后沿着半径为
的外柱面流回）。\\
(b)两条宽度为$w$，相距很小的距离$h<<w$薄金属“带”。电流沿着其中一
条流出，然后沿着另一条流回。电流在带的表面上是均匀分布的。}
(a)
\begin{align*}
    U&=\int_{a}^{b}\frac{\lambda}{4\pi\varepsilon_0 r}\dif r\\
    U&=\frac{\lambda}{4\pi\varepsilon_0}\ln\frac{b}{a}\\
    \lambda&=\frac{4\pi\varepsilon_0U}{\ln(b/a)}\\
    \int\boldsymbol{S}\cdot\dif\boldsymbol\sigma&=\int\frac{1}{\mu_0}\boldsymbol{E}\times\boldsymbol{B}\cdot\dif\boldsymbol\sigma\\
    \int\boldsymbol{S}\cdot\dif\boldsymbol\sigma&=\int\frac{1}{\mu_0}\frac{\lambda}{4\pi\varepsilon_0 r}\frac{\mu_0I}{2\pi r}\dif\sigma\\
    \int\boldsymbol{S}\cdot\dif\boldsymbol\sigma&=\int\frac{U}{r\ln(b/a)}\frac{I}{2\pi r}2\pi r\dif r\\
    \int\boldsymbol{S}\cdot\dif\boldsymbol\sigma&=UI
\end{align*}

(b)\begin{align*}
    S&=EB\\
    S&=\frac{U}{\mu_0 h}\mu_0J\\
    S&=\frac{UI}{wh}\\
    \int\boldsymbol{S}\cdot\dif\boldsymbol\sigma&=UI
\end{align*}
\mysssec{一个半径为$a$的粗导线，其横截面上均匀分布有稳恒电流$I$。导线中有一个宽度$\omega<<a$的
窄间隙，从而形成了一个平行板电容器。\\
（a）作为到轴线的距离$s$与时间$t$的函数，求间隙中的电场与磁场。（假设在$t$=0时刻，电容器的电荷量为零）。\\
（b)求间隙中的能量密度$u$。和坡印廷矢量$S$。并且验证是否满足式\ref{坡印廷_守恒定律}。
% //（c）作为时间的函数，计算间隙中的总能量。找一个合适的表面对坡印廷矢量积分求出电磁场向间隙中传输的总功率，检验它与间隙中能量的增加率相等。（式（8.9）—不过现在$W=0$，因为间隙中没有电荷）。[如果担心边缘区域的影响，可以选取半径$b<a$的体积使其完全处在间隙内部。]
}
(a)
\begin{align*}
    E&=\frac{Q}{\pi a^2\varepsilon_0}\\
    E&=\frac{It}{\pi a^2\varepsilon_0}\\
    2\pi sB&=\pi s^2\mu_0\varepsilon_0\frac{\partial E}{\partial t}\\
    2\pi sB&=\pi s^2\mu_0\frac{I}{\pi a^2}\\
    B&=\mu_0\frac{Is}{2\pi a^2}
\end{align*}

(b)\begin{align*}
    S&=\frac{It}{\pi a^2\varepsilon_0}\frac{Is}{2\pi a^2}\\
    \boldsymbol{\nabla}\cdot\boldsymbol{S}&=\frac{S_s}{s}+\dfrac{\partial S_s}{\partial s}+\frac{1}{s}\dfrac{\partial S_{\phi}}{\partial \phi}+\dfrac{\partial S_{z}}{\partial z}\\
    \boldsymbol{\nabla}\cdot\boldsymbol{S}&=\frac{S_s}{s}+\dfrac{\partial S_s}{\partial s}\\
    \boldsymbol{\nabla}\cdot\boldsymbol{S}&=-\frac{I^2t}{\pi^2 a^4\varepsilon_0}\\
    u&=\frac{1}{2}\varepsilon_0E^2+\frac{1}{2\mu_0}B^2\\
    u&=\frac{1}{2}\varepsilon_0\frac{I^2t^2}{\pi^2 a^4\varepsilon_0^2}+\frac{1}{2\mu_0}\mu_0\frac{I^2s^2}{4\pi^2 a^4}\\
    u&=\frac{1}{2}\frac{I^2t^2}{\pi^2 a^4\varepsilon_0}+\frac{1}{2}\mu_0\frac{I^2s^2}{4\pi^2 a^4}\\
    \frac{\partial u}{\partial t}&=\frac{I^2t}{\pi^2 a^4\varepsilon_0}
\end{align*}
\subsection{动量}
\subsubsection{麦克斯韦应力张量}
\begin{align*}
    \boldsymbol f&=\rho\boldsymbol{E}+\boldsymbol{J}\times\boldsymbol{B}\\
    \boldsymbol f&=\varepsilon_0\boldsymbol{\nabla}\cdot\boldsymbol{E}\boldsymbol{E}+\left(\frac{1}{\mu_0}\boldsymbol{\nabla}\times\boldsymbol{B}-\varepsilon_0\frac{\partial\boldsymbol{E}}{\partial t}\right)\times\boldsymbol{B}\\
    \boldsymbol f&=\varepsilon_0\boldsymbol{\nabla}\cdot\boldsymbol{E}\boldsymbol{E}+\frac{1}{\mu_0}\boldsymbol{\nabla}\times\boldsymbol{B}\times\boldsymbol{B}
    -\varepsilon_0\frac{\partial\boldsymbol{E}}{\partial t}\times\boldsymbol{B}\\
    \boldsymbol f&=\varepsilon_0\boldsymbol{\nabla}\cdot\boldsymbol{E}\boldsymbol{E}+\frac{1}{\mu_0}\boldsymbol{\nabla}\times\boldsymbol{B}\times\boldsymbol{B}
    -\varepsilon_0\frac{\partial\boldsymbol{E}\times\boldsymbol{B}}{\partial t}
    +\varepsilon_0\boldsymbol{E}\times\frac{\partial\boldsymbol{B}}{\partial t}\\
    \boldsymbol f&=\varepsilon_0\boldsymbol{\nabla}\cdot\boldsymbol{E}\boldsymbol{E}+\frac{1}{\mu_0}\boldsymbol{\nabla}\times\boldsymbol{B}\times\boldsymbol{B}
    -\varepsilon_0\frac{\partial\boldsymbol{E}\times\boldsymbol{B}}{\partial t}
    -\varepsilon_0\boldsymbol{E}\times\left(\boldsymbol{\nabla}\times\boldsymbol{E}\right)\\
    % \boldsymbol{\nabla}(\boldsymbol{E}\cdot\boldsymbol{E})&=2\boldsymbol{E}\times(\boldsymbol{\nabla}\times\boldsymbol{E})+
    % 2(\boldsymbol{E}\cdot\boldsymbol{\nabla})\boldsymbol{E}\\
    \boldsymbol f&=\varepsilon_0\left[\left(\boldsymbol{\nabla}\cdot\boldsymbol{E}\right)+\left(\boldsymbol{E}\cdot\boldsymbol{\nabla}\right)\right]\boldsymbol{E}
    +\frac{1}{\mu_0}\left[\left(\boldsymbol{\nabla}\cdot\boldsymbol{B}\right)+\left(\boldsymbol{B}\cdot\boldsymbol{\nabla}\right)\right]\boldsymbol{B}
    -\frac12\boldsymbol{\nabla}\left(\varepsilon_0E^2+\frac{1}{\mu_0}B^2\right)-\varepsilon_0\frac{\partial}{\partial t}(\boldsymbol{E}\times\boldsymbol{B})
\end{align*}
引入麦克斯韦应力张量
\begin{equation}
    T_{ij}\equiv\varepsilon_0\left(E_iE_j-\frac{1}{2}\delta_{ij}E^2\right)+\frac{1}{\mu_0}\left(B_iB_j-\frac{1}{2}\delta_{ij}B^2\right)
\end{equation}
\begin{align}
    \boldsymbol{\nabla}\cdot\boldsymbol{T}&=\frac{\partial}{\partial x_i}\varepsilon_0\left(E_iE_j-\frac{1}{2}\delta_{ij}E^2\right)\boldsymbol{e}_j
    +\frac{\partial}{\partial x_i}\frac{1}{\mu_0}\left(B_iB_j-\frac{1}{2}\delta_{ij}B^2\right)\boldsymbol{e}_j\nonumber\\
    \boldsymbol{\nabla}\cdot\boldsymbol{T}&=\varepsilon_0\frac{\partial E_iE_j}{\partial x_i}\boldsymbol{e}_j
    -\varepsilon_0\frac{1}{2}\frac{\partial E^2}{\partial x_i}\delta_{ij}\boldsymbol{e}_j
    +\frac{1}{\mu_0}\frac{\partial B_iB_j}{\partial x_i}\frac{1}{\mu_0}\boldsymbol{e}_j
    -\frac{1}{\mu_0}\frac{1}{2}\frac{\partial B^2}{\partial x_i}\delta_{ij}\boldsymbol{e}_j\nonumber\\
    \boldsymbol{\nabla}\cdot\boldsymbol{T}&=\varepsilon_0\frac{\partial E_iE_j}{\partial x_i}\boldsymbol{e}_j
    -\varepsilon_0\frac{1}{2}\frac{\partial E^2}{\partial x_i}\boldsymbol{e}_i
    +\frac{1}{\mu_0}\frac{\partial B_iB_j}{\partial x_i}\boldsymbol{e}_j
    -\frac{1}{\mu_0}\frac{1}{2}\frac{\partial B^2}{\partial x_i}\boldsymbol{e}_i\nonumber\\
    \boldsymbol{f}&=\boldsymbol{\nabla}\cdot\boldsymbol{T}-\varepsilon_0\mu_0\frac{\partial\boldsymbol S}{\partial t}\label{麦克斯韦应力张量受力}
\end{align}
动量密度$\boldsymbol{\wp}$
\begin{equation}
    \boldsymbol{\wp}=\varepsilon_0\mu_0\boldsymbol{S}=\varepsilon_0(\boldsymbol{E}\times\boldsymbol{B})
\end{equation}
\subsubsection{动量守恒}
\begin{align}
    \frac{\dif \boldsymbol{p}_{mech}}{\dif t}&=\boldsymbol{F}\\
    \frac{\dif \boldsymbol{p}_{mech}}{\dif t}&=\oint\boldsymbol{T}\cdot\dif\boldsymbol{a}-\varepsilon_0\mu_0\int\frac{\dif\boldsymbol S}{\dif t}\dif\tau
\end{align}
式中，$\boldsymbol{p}_{mech}$代表体积内所有粒子的总（机械）动量。
\subsubsection{角动量}
角动量密度$\ell$
\begin{equation}
    \ell=\boldsymbol{r}\times\boldsymbol{\wp}=\varepsilon_0\left[\boldsymbol{r}\times(\boldsymbol E\times\boldsymbol B)\right]
\end{equation}
\mysssec{求一个均匀带电球下半球作用于上半球的力，球的半径为$R$，带有电荷$q$。}
\begin{align*}
    F_z&=\oiint\boldsymbol{T}_z\cdot\dif\boldsymbol{a}\\
    F_z&=\iint\boldsymbol{T}_z\cdot\dif\boldsymbol{a}+\iint\boldsymbol{T}_z\cdot\dif\boldsymbol{a}'\\
    \iint\boldsymbol{T}_z\cdot\dif\boldsymbol{a}&=\iint\varepsilon_0\left(E_iE_z-\frac{1}{2}\delta_{iz}E^2\right)\dif a_i\\
    \iint\boldsymbol{T}_z\cdot\dif\boldsymbol{a}&=\varepsilon_0\iint E_iE_z\dif a_i
    -\frac{1}{2}\delta_{iz}E^2\dif a_i\\
    \iint\boldsymbol{T}_z\cdot\dif\boldsymbol{a}&=\varepsilon_0\iint
    E_z^2\dif a_z-\frac{1}{2}E^2\dif a_z\\
    \iint\boldsymbol{T}_z\cdot\dif\boldsymbol{a}&=\int_0^R
    \varepsilon_0\frac{1}{2}\left(\frac{qr^3}{4\pi\varepsilon_0 r^2R^3}\right)^22\pi r\dif r\\
    \iint\boldsymbol{T}_z\cdot\dif\boldsymbol{a}&=
    \frac{q^2}{64\pi\varepsilon_0R^2}\\
    \iint\boldsymbol{T}_z\cdot\dif\boldsymbol{a}'&=\iint\varepsilon_0\left(E_iE_z-\frac{1}{2}\delta_{iz}E^2\right)\dif a_i'\\
    \iint\boldsymbol{T}_z\cdot\dif\boldsymbol{a}'&=\iint \varepsilon_0E_iE_z\dif a_i'
    -\varepsilon_0\frac{1}{2}E^2\dif a_z'\\
    \boldsymbol{E}&\parallel \boldsymbol{a}'\\
    E_zE_ia'_i&=E_zE\\
    E_zE_ia'_i&=E^2a'_z\\
    \iint\boldsymbol{T}_z\cdot\dif\boldsymbol{a}'&=\iint \varepsilon_0\frac{1}{2}E^2\dif a_z'\\
    \iint\boldsymbol{T}_z\cdot\dif\boldsymbol{a}'&=\varepsilon_0\frac{1}{2}\left(\frac{q}{4\pi\varepsilon_0 R^2}\right)^2\pi R^2\\
    \iint\boldsymbol{T}_z\cdot\dif\boldsymbol{a}'&=\frac{q^2}{32\pi\varepsilon_0 R^2}\\
    F_z&=\frac{3q^2}{64\pi\varepsilon_0 R^2}
\end{align*}
\mysssec{计算一个均匀带电旋转球壳南北半球间的磁吸引力。球壳半径为$R$，转动角速度为$\omega$，电荷面密度为$\sigma$。($\boldsymbol{B}_{\text{内}}=\frac{2}{3}
\mu_0\sigma\omega R \boldsymbol{e}_z,\ \boldsymbol{B}_{\text{z外}}=-\frac{1}{3r^3}
\mu_0\sigma\omega R^4 \boldsymbol{e}_z$)}
\begin{align*}
    F_z&=\frac{1}{\mu_0}\iint\left(B_iB_z-\frac{1}{2}\delta_{iz}B^2\right)\dif a_i\\
    F_z&=\frac{1}{\mu_0}\iint B_iB_z\dif a_i-\frac{1}{2}\delta_{iz}B^2\dif a_i\\
    F_z&=\frac{1}{\mu_0}\iint\frac{1}{2}B^2\dif a_z\\
    F_z&=\frac{1}{\mu_0}\iint\frac{1}{2}\left(\frac{2}{3}\mu_0\sigma\omega R \right)^2\dif a_z
    +\frac{1}{\mu_0}\int_R^\infty\frac{1}{2}\left(\frac{1}{3r^3}\mu_0\sigma\omega R^4\right)^22\pi r\dif r\\
    F_z&=\iint\frac{2}{9}\mu_0\sigma^2\omega^2 R^2\dif a_z
    +\int_R^\infty\frac{1}{9r^5}\mu_0\sigma^2\omega^2 R^8\pi \dif r\\
    F_z&=-\frac{2}{9}\mu_0\sigma^2\omega^2 R^4\pi-\frac{1}{36}\mu_0\sigma^2\omega^2 R^4\pi\\
    F_z&=-\frac{1}{4}\mu_0\sigma^2\omega^2 R^4\pi
\end{align*}
\mysssec{考虑相距为$2a$，带有等量电荷量$q$的两个点电荷。在到两电荷距离相等的平面上对麦克斯韦应力张量积分求一个电荷对另一个的作用力。\\
（b）若两电荷带等量相反电荷，重复（a)中的求解。}
(a)\begin{align*}
    F_z&=\iint\varepsilon_0\left(E_iE_z-\frac{1}{2}\delta_{iz}E^2\right)\dif a_i\\
    F_z&=-\iint\varepsilon_0\frac{1}{2}E^2\dif a_z\\
    F_z&=\int_0^\infty\varepsilon_0\frac{1}{2}\left[\frac{2rq}{4\pi \varepsilon_0(a^2+r^2)^\frac32}\right]^22\pi r\dif r\\
    F_z&=\frac{q^2}{16 a^2\pi\varepsilon_0}
\end{align*}
(b)
\begin{align*}
    F_z&=\iint\varepsilon_0\left(E_iE_z-\frac{1}{2}\delta_{iz}E^2\right)\dif a_i\\
    F_z&=\iint\varepsilon_0\frac{1}{2}E^2\dif a_z\\
    F_z&=-\int_0^\infty\varepsilon_0\frac{1}{2}\left[\frac{2aq}{4\pi\varepsilon_0(a^2+r^2)^\frac32}\right]^22\pi r\dif r\\
    F_z&=-\int_0^\infty\frac{a^2q^2}{4\pi\varepsilon_0(a^2+r^2)^3}r\dif r\\
    F_z&=-\frac{q^2}{16 a^2\pi\varepsilon_0}
\end{align*}
\mysssec{长为$l$的长同轴电缆由内导线（半径为$a$）和外导线（半径为$b$)组成。在导线一端接有电池，另一端接有电阻。内导线带有均匀分布的电荷，每单位长度上电荷量为$\lambda$，并且载有稳定向右的电流$I$；外导线具有相反的电荷和电流。问：电磁场具有的动量为多少?}
\begin{align*}
    \boldsymbol{p}_{em}&=\mu_0\varepsilon_0\int\boldsymbol{S}\dif\tau\\
    \boldsymbol{p}_{em}&=\varepsilon_0\int\boldsymbol{E}\times\boldsymbol{B}\dif\tau\\
    \boldsymbol{p}_{em}&=\varepsilon_0\int_a^b\frac{\lambda}{2\pi\varepsilon s}\frac{\mu_0I}{2\pi s}\boldsymbol{e}_s\times\boldsymbol{e}_\phi2\pi s\dif s\\
    \boldsymbol{p}_{em}&=\varepsilon_0\int_a^b\frac{\lambda\mu_0I}{2\pi\varepsilon s}\boldsymbol{e}_z\dif s\\
    \boldsymbol{p}_{em}&=\varepsilon_0\frac{\lambda\mu_0I}{2\pi\varepsilon}\boldsymbol{e}_z\ln\frac{b}{a}
\end{align*}
\mysssec{考虑一个无限大平板电容器，下板面（$z=-d/2$)电荷面密度为$-\sigma$，上板面（$z=+d/2$）电荷面密度为$+\sigma$。\\
（a）求两板之间的电磁场应力张量的九个分量。将结果写成一个$3\times3$矩阵的形式。\\
（b)利用式\ref{麦克斯韦应力张量受力}求作用在上板面单位面积上的力。\\
（c)求单位时间内穿过$xy$平面单位面积的动量。}
(a)\begin{align*}
    T_{ij}&=\left(E_iE_j-\frac{1}{2}\delta_{ij}E^2\right)\\
    T&=\left(\begin{matrix}
        T_{xx}&T_{xy}&T_{xz}\\
        T_{yx}&T_{yy}&T_{yz}\\
        T_{zx}&T_{zy}&T_{zz}\\
    \end{matrix}\right)\\
    T&=\varepsilon_0\left(\begin{matrix}
        E_xE_x-\frac{1}{2}E^2&E_xE_y&E_xE_z\\
        E_yE_x&E_yE_y-\frac{1}{2}E^2&E_yE_z\\
        E_zxE_j&E_zE_y&E_zE_z-\frac{1}{2}E^2\\
    \end{matrix}\right)\\
    T&=\varepsilon_0\left(\begin{matrix}
        -\frac{1}{2}E^2&0&0\\
        0&-\frac{1}{2}E^2&0\\
        0&0&\frac{1}{2}E^2\\
    \end{matrix}\right)\\
    T&=\left(\begin{matrix}
        -\dfrac{\sigma^2}{2\varepsilon_0}&0&0\\
        0&-\dfrac{\sigma^2}{2\varepsilon_0}&0\\
        0&0&\dfrac{\sigma^2}{2\varepsilon_0}\\
    \end{matrix}\right)
\end{align*}

(b)\begin{align*}
    \boldsymbol{F}&=-\boldsymbol{T}\cdot\dif\boldsymbol{a}\\
    \boldsymbol{F}&=-\iint\dfrac{\sigma^2}{2\varepsilon_0}\dif{a}\\
    \boldsymbol{F}&=-\dfrac{\sigma^2}{2\varepsilon_0}
\end{align*}

(c)\begin{align*}
    \boldsymbol{p}_z&=-\boldsymbol{T}\\
    \boldsymbol{p}_z&=-\dfrac{\sigma^2}{2\varepsilon_0}\boldsymbol{e}_z
\end{align*}

\mysssec{一个带电平行板电容器（板间有均匀电场$\boldsymbol{E}=E\boldsymbol{e}_z$）放置在均匀磁场$\boldsymbol{B}=B\boldsymbol{e}_x$中。\\
（a）求两板之间的电磁动量。\\
（b）用一个具有电阻的导线连接两平板，沿着$z$轴方向，
电容器开始缓慢放电。导线中的电流会受到磁力；求放电过程
中系统受到的总冲量？\\
（c）缓慢减小磁场。这样会产生一个法拉第电场，它将会对平板施加
一个作用力。求整个过程中对系统施加的总冲量。}
(a)\begin{align*}
    \boldsymbol{p}_{em}&=\varepsilon_0\int\boldsymbol{E}\times\boldsymbol{B}\dif\tau\\
    \boldsymbol{p}_{em}&=\varepsilon_0\int E\boldsymbol{e}_z\times B\boldsymbol{e}_x\dif\tau\\
\boldsymbol{p}_{em}&=-\varepsilon_0LBE\boldsymbol{e}_y
\end{align*}

(b)\begin{align*}
    \boldsymbol{F}&=I\boldsymbol{B}\times\boldsymbol{L}\\
    \boldsymbol{F}t&=-QBL\boldsymbol{e}_y\\
    \boldsymbol{F}t&=-\varepsilon_0LBE\boldsymbol{e}_y
\end{align*}

(c)\begin{align*}
    \boldsymbol{\nabla}\times\boldsymbol{E}&=-\frac{\partial \boldsymbol{B}}{\partial t}\\
    t\oint\boldsymbol{E}\dif s&=-\boldsymbol{B}l\\
    tE&=-BL\\
    Ft&=\sigma Et\\
    Ft&=-\varepsilon_0E BL
\end{align*}
\mysssec{设想有一个半径为$R$的长螺线管线圈，单位长度绕有$n$匝，载有电流$I$。有两个长为$l$的圆柱形壳管与螺线管同轴，螺线管内的壳管半径为$a$，表面均匀带有电荷$+Q$。螺线管外的壳管半径为$b$，均匀带有电荷$-Q$(设$l>>b$)。当螺线管中电流逐渐减小时，圆柱形壳管开始旋转。问：这些角动量来自哪里？}
\begin{align*}
    \iiint\ell&=\iiint\varepsilon_0\left[\boldsymbol{r}\times(\boldsymbol E\times\boldsymbol B)\right]\dif\tau\\
    \iiint\ell&=2\pi\int_a^R\varepsilon_0\left[s\boldsymbol{e}_r\times\left(\frac{Q}{2\pi s}\boldsymbol{e}_r\times\mu_0nI\boldsymbol{e}_z s\right)\right]\dif s\\
    \iiint\ell&=-\int_a^R\varepsilon_0s\boldsymbol{e}_r\times Q\boldsymbol{e}_\phi\mu_0nI\dif s\\
    \iiint\ell&=-\varepsilon_0\boldsymbol{e}_zQ\mu_0nI(R^2-a^2)\\
\end{align*}
\mysssec{设想一个半径为$R$的均匀磁化铁球，带有电荷$Q$和磁矩$\boldsymbol M=M\boldsymbol{e}_z$。铁球开始时处于静止。\\
（a）计算存储在电磁场中的角动量。\\
（b）假设该球被逐渐（均匀）去磁（或许加热到居里温度以上）。用法拉第定律确定感应电场，求出这个电场作用在球体上的力矩，求出在整个去磁过程中传递给球体的总角动量。\\
（c）如果代替去磁，我们在球体北极连接一个接地导线使球体放电。假设球面上电流的流动使电荷密度保持均匀。用洛伦兹力定律确定作用在球上的力矩，并且计算整个放电过程中传递给球的力矩。（球面处的磁场并不连续，这有影响吗?}
(a)\begin{align*}
    \boldsymbol{L}
\end{align*}